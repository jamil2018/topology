services:
  postgres:
    image: postgres:16-alpine
    container_name: topology-postgres
    restart: unless-stopped
    ports:
      - "54329:5432"
    environment:
      POSTGRES_USER: topology
      POSTGRES_PASSWORD: topology
      POSTGRES_DB: topology
    volumes:
      - topology_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U topology -d topology"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  topology_pgdata:
    name: topology_pgdata
  # Named volume for durable result attachments when mounting into an app container.
  # Local Next.js default path is ./data/attachments (see ATTACHMENTS_DIR).
  topology_attachments:
