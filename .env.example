# App
AUTH_SECRET=generate-with-openssl-rand-base64-32
AUTH_URL=http://127.0.0.1:4317

# Database (matches docker-compose.yml)
DATABASE_URL=postgresql://topology:topology@127.0.0.1:54329/topology

# OAuth-first (optional). When unset, login uses email/password (air-gap path).
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Force air-gap messaging even if OAuth ids are present:
# TOPOLOGY_AIR_GAP=1

# Local demo user (seeded on `npm run db:seed`)
DEMO_USER_EMAIL=demo@topology.local
DEMO_USER_PASSWORD=topology-demo

# CLI / CI / agent API token (Bearer for /api/ci/* and /api/agent)
TOPOLOGY_API_TOKEN=topo_demo_token_local_dev_only
TOPOLOGY_URL=http://127.0.0.1:4317

# Result attachments (local volume; default ./data/attachments)
ATTACHMENTS_DIR=./data/attachments

# Issue providers — default mock (no network). Set jira|linear|github + credentials to go live.
# Keep tracker secrets server-side only (never ship to the browser or MCP env beyond TOPOLOGY_API_TOKEN).
TOPOLOGY_ISSUE_PROVIDER=mock
# JIRA_BASE_URL=https://your-org.atlassian.net
# JIRA_EMAIL=
# JIRA_API_TOKEN=
# JIRA_PROJECT_KEY=
# LINEAR_API_KEY=
# LINEAR_TEAM_ID=
# GITHUB_TOKEN=
# GITHUB_REPO=owner/repo
